[package]
name = "abc169"
version = "0.1.0"
authors = ["qwerty2501 <qwerty2501@live.jp>"]
edition = "2018"

# dependencies added to new project
[dependencies]
rust_decimal = "*"

[profile.release]
lto = true
panic = 'abort'
